export interface Phase {
  id: number;
  name: string;
  age: string;
  minSalary: number;
  minPortfolio: number;
  description: string;
}

export interface BtcConditions {
  disposable: boolean;
  dcaActive: boolean;
}
