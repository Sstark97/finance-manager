# Optional for local development: if unset, "pnpm dev" falls back to a local SQLite database
# (see scripts/setup-local-dev-db.mjs). Required in production — the app fails to start without it.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
